node_modules/
dist/
.env
.env.local
.DS_Store
*.log
.vite/
*.tsbuildinfo
web/dist/
web/node_modules/

# Business data files (contain internal info — do not commit)
*.xlsx
*.docx
Sup/
.claude/

# Credentials — never commit
users-passwords*.csv
users-passwords*.xlsx
*-passwords.*
# Ignore Windows system files
desktop.ini

# Legacy defect photos pulled from Google Drive for the one-off migration
# (~460 MB). Never belongs in the repo.
Order Details_Imagess/
Order*Details*Image*/
scripts/.image-migration.json
