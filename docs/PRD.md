# Product Requirements Document (PRD)
# QC Inspection — ระบบสุ่มตรวจคุณภาพ

**Version:** 2.0
**Last Updated:** 28 เมษายน 2026
**Owner:** Comets Intertrade Co., Ltd.
**Status:** Active (Production)
**Live URL:** https://web-mocha-three-44.vercel.app

---

## 1. Executive Summary

**QC Inspection** เป็น Web Application สำหรับบันทึกและจัดการผลการสุ่มตรวจคุณภาพสินค้าของ Comets Intertrade รวมถึงรายงาน NCR (Non-Conformance Report) อัตโนมัติเมื่อพบของไม่ผ่านมาตรฐาน ระบบรวมศูนย์ข้อมูลใน Supabase (PostgreSQL) ควบคุมสิทธิ์ตาม Role และมี Dashboard วิเคราะห์ภาพรวมพร้อมส่งออก Excel/PDF

### Goals
- บันทึกการสุ่มตรวจคุณภาพในฐานข้อมูลเดียว (ทดแทน Excel/กระดาษ)
- Auto-fill จาก Master Data → ลดเวลากรอกข้อมูล
- ควบคุมการแก้ไขข้อมูลด้วย Approval Workflow (audit trail ครบ)
- หลักฐานภาพถ่ายของเสีย 1-3 รูป/รายการ
- รายงาน % ของเสีย + Critical/Major/Minor อัตโนมัติ
- Dashboard วิเคราะห์ตาม Supplier / Brand / ช่วงเวลา + Export
- จัดการ Master Data (Suppliers / Materials / Defects) ในตัว

### Success Metrics
- บันทึก QC Order ≤ 3 นาที/ใบ
- ความถูกต้อง Auto-fill (จาก SAP Code) ≥ 95%
- Admin จัดการ User / Master Data ได้เองโดยไม่ต้องติดต่อ IT
- Dashboard ตอบสนอง ≤ 2s แม้มี data > 5,000 orders

---

## 2. Target Users (Roles)

| Role | ชื่อในระบบ | สิทธิ์โดยสรุป |
|---|---|---|
| **Admin System** | `admin` | ทุกอย่าง — รวมจัดการ User |
| **QC Admin** | `qc_admin` | จัดการ Master Data + อนุมัติ + Dashboard + Material |
| **QC Staff** | `operator` | บันทึก QC + ดูประวัติ + แก้ไขเฉพาะของตัวเอง (เมื่อ admin อนุมัติ) |
| **Viewer** | `viewer` | ดู History + Dashboard + Material อย่างเดียว |

> รองรับการเพิ่ม role ใหม่ได้แบบ free-text (text column) — RLS ใช้ helper `current_role_level()`

---

## 3. Pages & Navigation Map

| Path | หน้า | Roles ที่เข้าถึง |
|---|---|---|
| `/login` | Login | ทุกคน |
| `/` | History (หน้าแรกหลัง login) | ทุก role ที่ login |
| `/entry` | บันทึก QC | ทุก role (แต่ viewer ไม่ควรเข้า — UI ไม่ปรากฏปุ่มหลักให้) |
| `/edit/:orderId` | แก้ไข QC Order (เมื่อ `edit_approved=true`) | เจ้าของ order หรือ admin/qc_admin |
| `/dashboard` | Dashboard + Charts + Export | admin, qc_admin, viewer |
| `/materials` | Material Management | ทุกคน (ดู), admin/qc_admin (อัปโหลด) |
| `/admin` | Admin Panel (Suppliers / Defects / Users) | admin, qc_admin (เฉพาะ admin จัดการ Users) |
| `/guide` | คู่มือใช้งาน | ทุกคน (เนื้อหาตาม role) |

### Top Navigation
```
ประวัติ / History  →  บันทึก QC / Entry  →  Dashboard*  →  Material  →  จัดการ / Admin*  →  คู่มือ / Guide
```
\* แสดงตามสิทธิ์

---

## 4. Feature Specifications

### 4.1 Authentication
- **Login:** Email + Password (สร้างโดย Admin System เท่านั้น)
- **Session:** ไม่เก็บ session (`persistSession: false`) — Login ใหม่ทุกครั้งที่เปิดเว็บ
- **Logout:** ปุ่ม "ออก / Logout" ที่ Header
- **Password Policy:** เมื่อตั้งครั้งแรกแล้ว **ไม่มี UI ให้เปลี่ยน/รีเซ็ต** — ถ้าลืมต้องลบ user แล้วสร้างใหม่

### 4.2 บันทึก QC (QC Entry — `/entry`)

**Header Fields:**
| Field | Type | Auto-fill Source | Required |
|---|---|---|---|
| วันที่ / Date | Date | วันนี้ | ✅ |
| Order Status (Inspection Result) | Button group | — | ✅ (Accept / Accept Lot / Reject) |
| SAP Code | Text | — | ✅ |
| รายละเอียดสินค้า | Text | materials.description | display |
| กลุ่มสินค้า | Text | materials.product_category | display |
| Brand | Text | materials.brand | display |
| Sales | Text (editable) | materials.sales | ✅ |
| SCM | Text (editable) | materials.scm | ✅ |
| Sup SAP / Sup Code / Supplier | Text | suppliers (lookup) | display |
| Lot No. | Text | — | optional |
| จำนวนรับ / Received Qty | Number | — | optional |
| จำนวนตรวจสอบ / Sample Size | Number | — | ✅ |
| หมายเหตุ / Note | Textarea | — | optional |

**Defect List (multi-select):**
- ค้นหารหัสของเสียและอาการ → เลือกได้หลายอันพร้อมกัน → "เพิ่มในรายการ" → รวมเป็น 1 แถว
- ระดับ: Critical / Major / Minor
- จำนวน: Integer
- รูปภาพ: 1-3 รูป/แถว (เก็บใน Supabase Storage bucket `defect-images`)

**Real-time Calculations:**
- `defect_qty = Σ quantity ของ defect ทั้งหมด`
- `good_qty = sample_size − defect_qty`
- `defect_percent = defect_qty / sample_size × 100` (generated column ใน DB)
- `critical_qty / major_qty / minor_qty` แยกตาม rank

**Status Buttons (Inspection Result):**
- ✅ ผ่าน / Accept
- 🟡 รับ Lot / Accept Lot
- ❌ ไม่ผ่าน / Reject — **ทริกเกอร์สร้าง NCR อัตโนมัติ**

**Post-Save:**
- Popup สรุปข้อมูล (read-only)
- Order No: `QC<YY><MM><seq4>` (เช่น `QC26040001`) — auto-generated by trigger
- Reset form

### 4.3 ประวัติ (History — `/`)

**Layout:**
- Header: ค้นหา + Filter Status + ปุ่ม "บันทึกใหม่"
- รายการ QC Orders **จัดกลุ่มตามสถานะ** (sticky group header):
  - ✓ อนุมัติแล้ว / Approved
  - ผ่าน / Accept
  - รับ Lot / Accept Lot
  - ❌ ไม่ผ่าน / Reject
  - ✏️ รอแก้ไข / Pending Edit
  - อื่น ๆ / Other (legacy)

**Each Card shows:**
- Order No, วันที่, Inspection Result chip, Approval chip (Pending/Approved), Edit chip (ถ้ามี)
- SAP Code + รายละเอียด, Brand, Supplier, Lot
- % ของเสีย + Critical/Major/Minor counts

**Expanded View:**
- Header info ครบทุก field
- Edit reason (ถ้า admin อนุมัติแก้ไข)
- รายการของเสีย + รูปภาพ

**Action Buttons (per role + per state):**
- **Admin/QC Admin** (เมื่อยังไม่อนุมัติและไม่อยู่ในสถานะแก้ไข):
  - ✓ **อนุมัติ / Approve** — set `approved=true`
  - ✏️ **ต้องแก้ไข / Need Edit** — popup ขอเหตุผล → set `edit_approved=true`
- **Operator/Owner** (เมื่อ `edit_approved=true`):
  - **แก้ไขข้อมูล / Edit** → ไป `/edit/:orderId` (เฉพาะเจ้าของ order หรือ admin)

**Two Independent States** (สำคัญ):
- `inspection_result` (Accept / Accept Lot / Reject) — กำหนดโดย Operator
- `approval_status` (Pending / Approved) — กำหนดโดย QC Admin
- ทั้งสองแยกกันอย่างสิ้นเชิง — ไม่ปะปนกัน

### 4.4 แก้ไข QC Order (`/edit/:orderId`)

**เงื่อนไขเข้าถึง:**
- `order.edit_approved === true` (admin ปลดล็อกแล้ว)
- ผู้ใช้คือ **เจ้าของ order** (`created_by === user.id`) หรือ admin/qc_admin
- ถ้าไม่ตรง → alert แล้ว redirect กลับ History

**Workflow:**
1. Admin/QC Admin คลิก "ต้องแก้ไข / Need Edit" → popup ขอเหตุผล (required)
2. ระบบ set `edit_approved=true`, บันทึก `edit_reason`, `edit_approved_by`, `edit_approved_at`
3. เจ้าของ order เห็นปุ่ม "แก้ไขข้อมูล" → ไปหน้า Edit
4. แก้ไขได้ทุก field ยกเว้น `order_no` และ `order_date`
5. บันทึก → reset `edit_approved=false`, `qc_order_edit_log` บันทึก `edited_by`, `edited_at`

### 4.5 Dashboard (`/dashboard`)

**Filter Bar (compact):**
- Date Range (From / To)
- Supplier / Brand / Product (search) / Inspector (created_by)

**KPI Cards:**
- Total Orders / Accept (รวม Accept Lot) / Reject / Avg Defect %

**Charts:**
- 📈 Defect Rate Trend (by month) — Line chart
- 🥧 Inspection Result Distribution — Pie chart (Accept / Accept Lot / Reject)
- 📊 Top Suppliers by Defect Rate — Bar chart (top 10)
- 📊 Top Defects by Quantity — Bar chart (top 10)

**Supplier Scorecard:**
- เมื่อเลือก Supplier ใน filter → แสดง scorecard ของ supplier นั้น (Accept rate, Defect %, top defect codes)

**Export:**
- 📊 Excel — Summary + Orders + Top Suppliers + Top Defects (4 sheets)
- 📄 PDF — Snapshot ของหน้า Dashboard (ใช้ html2canvas + jsPDF)

### 4.6 Material Management (`/materials`)

**Top Section:**
- Last Upload info: วันที่ + ผู้อัปโหลด + ชื่อไฟล์ + chip new/updated/error
- ⚠️ **Warning** ถ้าไม่อัปเดต > 7 วัน (เห็นเฉพาะ admin/qc_admin)
- ปุ่ม **📤 Upload Material File** (.xlsx) — เฉพาะ admin/qc_admin

**Preview Before Import:**
- Auto-detect header row (หา cell "Material ID")
- แสดง 50 แถวแรก พร้อม highlight: 🟢 New / 🟡 Update / 🔴 Error (missing/duplicate)
- Summary: Total / New / Update / Error
- ปุ่ม **ยืนยันนำเข้า / Confirm Import** + **ยกเลิก / Cancel**

**Import Logic:**
- Upsert by `sap_code` (Material ID = SAP Code) — chunks of 500
- Update fields: `description`, `product_category`, `base_uom`, `product_category_id`, `updated_by`
- `updated_at` ตั้งค่าโดย DB trigger
- Log ลง `material_upload_log` (file_name, uploaded_by, total/inserted/updated/error counts)

**Data Table:**
- Search (Material ID / Description) + Filter by Category + Sort columns
- แสดง 1,000 แถวแรก (ใช้ filter เพิ่มถ้าต้องการ)

### 4.7 Admin Panel (`/admin`)

**Tab: Suppliers** (CRUD โดย admin/qc_admin)
- Fields: Sup Code*, SAP Code, Supplier Name*, Category, Status (ACTIVE/INACTIVE), Purchase (Import/Local)

**Tab: รหัสของเสีย / Defect Codes** (CRUD)
- Fields (ตามลำดับ): Type → Reason → Running No. (Code) → Symptom (อาการ)
- Type/Reason เป็น combo field — ค่ามาตรฐาน + ค่าเดิมจาก DB (เพิ่มค่าใหม่ได้)
- มาตรฐาน SD-QC-1909-004-00 Rev02:
  - 6 Types: Defect, Repair, Short Shipment, Scrap, Supplier, Customer
  - 6 Reasons: Logo, Appearance, Function, Component, Bulk, Machine

**Tab: Users** (เฉพาะ admin)
- ดู: full_name, email, role
- เพิ่ม user ใหม่ (POST /api/admin-users) — กรอก Email + Password + Role + Full Name
- แก้ไข Full Name + Role (ไม่มีฟิลด์เปลี่ยนรหัส)
- ลบ user (ยกเว้นตัวเอง)

### 4.8 NCR (Non-Conformance Report)
- เมื่อ Operator บันทึก order ที่ `status='Reject'` → DB trigger สร้าง NCR record อัตโนมัติ
- ใช้ตาราง `ncr_reports` (อ้างอิง `qc_orders.id`)
- Order ที่ create NCR แล้ว — Success popup แจ้งให้ทราบ

### 4.9 Guide (`/guide`)
- คู่มือ in-app, เนื้อหาตาม role
- Operator: Login → บันทึก QC → ประวัติ → FAQ
- QC Admin: + Master Data + Approval workflow + Dashboard
- Admin: ครบ + จัดการ Users

---

## 5. Data Model

### Core Tables (PostgreSQL via Supabase)

**`profiles`** (extends `auth.users`)
- `id` UUID PK / FK `auth.users.id`
- `email`, `full_name`, `role text`

**`suppliers`**
- `id` bigserial PK
- `sup_code unique`, `sup_sap_code`, `supplier_name*`, `category`, `status`, `purchase`

**`materials`** (Master Data, ~18,723 records)
- `sap_code text PK` (= Material ID)
- `description`, `product_category`, `base_uom`, `product_category_id`
- `brand`, `sales`, `scm`
- `created_at`, `updated_at`, `updated_by`
- Trigger: `trg_materials_updated_at` (auto-update on insert/update)

**`material_upload_log`** (Audit log)
- `id` bigserial PK
- `file_name`, `uploaded_by`, `uploaded_at`
- `total_rows`, `inserted_count`, `updated_count`, `error_count`

**`defects`** (~4,536 records)
- `defect_code text PK`, `symptom*`, `reason`, `type`

**`brand_responsibilities`** (Sales/SCM ownership by Brand)
- `brand text PK`, `sales`, `scm`

**`qc_orders`**
- `id` bigserial PK, `order_no unique` (auto: `QC<YY><MM><seq4>`)
- `order_date`, `sap_code`, `material_description`, `brand`, `sales`, `scm`
- `sup_code`, `supplier_name`, `lot_no`
- `received_qty`, `sample_size*`, `good_qty`, `defect_qty`
- `critical_qty`, `major_qty`, `minor_qty`, `defect_percent` (generated column)
- `status` (Accept / Accept Lot / Reject) — inspection result
- `note`, `created_by` FK profiles, `created_at`
- **Edit tracking:** `edit_approved`, `edit_reason`, `edit_approved_by`, `edit_approved_at`
- **Approve tracking:** `approved`, `approved_by`, `approved_at`

**`qc_order_details`**
- `id` PK, `order_id` FK ON DELETE CASCADE
- `defect_code` (comma-separated possible), `symptom`
- `critical_rank` (Critical/Major/Minor), `quantity`
- `images text[]`

**`qc_order_edit_log`**
- `id`, `order_id`, `edit_reason*`, `approved_by`, `approved_at`, `edited_by`, `edited_at`

**`ncr_reports`** (Auto-created on Reject)
- `id`, `order_id` FK qc_orders, `ncr_no`, `created_at`, `status`

### Storage
- Bucket: `defect-images` (public read)
- Path: `<order_id>/<detail_idx>-<timestamp>-<random>.<ext>`

### Key Database Triggers
- `trg_qc_details_sync` — auto-recalc totals on detail insert/update/delete
- `gen_order_no` — auto-generate `order_no` on qc_orders insert
- `trg_create_ncr_on_reject` — auto-insert into `ncr_reports` when status changes to 'Reject'
- `trg_materials_updated_at` — auto-set `updated_at` on materials insert/update

---

## 6. Security & Access Control

### Row Level Security (RLS)
ทุกตารางเปิด RLS — ใช้ helper function `current_role_level()` คืน role จาก `profiles`

| Table | Read | Insert | Update | Delete |
|---|---|---|---|---|
| profiles | self + admin | admin | admin | admin |
| suppliers | authenticated | admin/qc_admin | admin/qc_admin | admin/qc_admin |
| materials | authenticated | admin/qc_admin | admin/qc_admin | admin/qc_admin |
| material_upload_log | authenticated | admin/qc_admin | admin/qc_admin | admin/qc_admin |
| defects | authenticated | admin/qc_admin | admin/qc_admin | admin/qc_admin |
| qc_orders | authenticated | auth.uid = created_by | own or admin | admin/qc_admin |
| qc_order_details | authenticated | owner of order | owner or admin | cascade |
| ncr_reports | authenticated | trigger only | admin/qc_admin | admin |

### API Security
- **Publishable Key (anon):** ใช้ใน client — ทุก mutation จะถูกบังคับด้วย RLS
- **Secret Key:** เก็บฝั่ง server — ใช้ใน Vercel Serverless Function `/api/admin-users` สำหรับสร้าง/แก้ไข/ลบ User เท่านั้น
- API endpoint ตรวจ token + role ก่อนทุก request (admin only)

### Password Policy
- ตั้งครั้งเดียวตอน Add User
- ไม่มี UI ให้เปลี่ยน/รีเซ็ต — ถ้าลืม → admin ลบ user แล้วสร้างใหม่ด้วย email เดิม
- Min 6 ตัวอักษร (Supabase Auth requirement)

---

## 7. Technical Stack

### Frontend
- **Framework:** React 18 + TypeScript
- **Build:** Vite 5
- **Styling:** TailwindCSS (custom design system "The Precision Ledger")
- **Routing:** React Router v6
- **State:** React hooks + Context (AuthProvider)
- **Charts:** Recharts
- **Excel:** SheetJS (xlsx)
- **PDF:** jsPDF + html2canvas

### Backend
- **Database:** PostgreSQL (Supabase)
- **Auth:** Supabase Auth (Email/Password, no OAuth)
- **Storage:** Supabase Storage (defect-images bucket)
- **API:** Vercel Serverless Functions (`/api/admin-users`)

### Deployment
- **Hosting:** Vercel
- **Stable URL:** https://web-mocha-three-44.vercel.app
- **Source:** https://github.com/Datacomets/QC
- **CI/CD:** Auto-deploy on push to main

### Design System
- **Display font:** Manrope (headings)
- **Body font:** Inter (data, labels)
- **Color:** surface layers, primary blue, error red, amber (edit)
- **Principles:** Intentional Asymmetry, Tonal Depth, no 1px solid borders

---

## 8. Non-Functional Requirements

### Performance
- Initial page load < 3s
- Search/autocomplete < 500ms
- Image upload < 10s/image
- Auth timeout 6s safety net (กำหนดใน auth.tsx)
- Dashboard render < 2s with up to 5,000 orders

### Compatibility
- Modern browsers (Chrome 90+, Edge 90+, Safari 14+)
- Mobile responsive (Tailwind breakpoints sm/md/lg)

### Localization
- ทุก label เป็น **bilingual** (ไทย / English)
- รูปแบบวันที่: **DD-MM-YYYY**
- Order No: `QC<YY><MM><seq4>`

### Data Retention
- QC Orders / Details / Images: indefinite
- Edit log: permanent (audit trail)
- Material upload log: permanent

---

## 9. Done in v2.0 (เพิ่มจาก v1.0)

- ✅ **Order Status (Inspection Result)** — Accept / Accept Lot / Reject (เอา Pending ออก)
- ✅ **Approve vs Need Edit แยกปุ่ม** — separate workflow
- ✅ **NCR auto-creation** เมื่อ status = Reject
- ✅ **Dashboard** + Recharts + Filters + Supplier Scorecard
- ✅ **Excel/PDF Export** จาก Dashboard
- ✅ **History Group by Status** (sticky group header + count badge)
- ✅ **Filter by Status** ใน History
- ✅ **Material Management** — Excel upload preview/import + log + 7-day stale warning
- ✅ **Inspection Result vs Approval Status** แยกอย่างชัดเจน (สอง chip แยก)
- ✅ **Operator แก้ไขเฉพาะ order ของตัวเอง** (owner check)
- ✅ **Viewer role** (ดู History/Dashboard/Material อย่างเดียว)
- ✅ **Custom Roles** (text-based, ไม่ใช่ enum แล้ว)
- ✅ **Password Locked** policy + drop `public.users.password` (security)

---

## 10. Out of Scope (V2)

- Email notifications (เช่น แจ้ง admin เมื่อมีคำขอแก้ไข)
- Bulk import QC orders จาก Excel
- Multi-language beyond Thai/English
- Mobile native app
- SSO (Microsoft/Google)
- Multi-approver workflow (QC → QA → Manager)
- Real-time SAP integration

---

## 11. Future Roadmap

### Phase 3 (Q3-Q4 2026)
- Email notification เมื่อ Admin อนุมัติแก้ไข / Reject เกิดขึ้น
- NCR module ขยาย (root cause, corrective action, follow-up)
- Bulk import QC orders จาก Excel
- Barcode/QR scanner สำหรับกรอก SAP/Lot

### Phase 4 (2027)
- Mobile app (React Native หรือ PWA installable)
- Multi-approver workflow (QC → QA → Manager)
- SAP integration ผ่าน API (real-time master data sync)

---

## 12. Initial Data (สถานะปัจจุบัน)

**Users:** 8 active accounts
- 1 admin (Admin System)
- 1 qc_admin (QC Admin)
- 2 operator (QC Staff)
- 4 viewer

> รหัสผ่านไม่เก็บในเอกสารนี้ — admin (sls03) ถือไว้

**Master Data (Imported from Excel):**
- **Suppliers:** ~90 records
- **Materials:** ~18,723 records (managed via Material Management page)
- **Defect Codes:** ~4,536 records (ตาม SD-QC-1909-004-00 Rev02)

---

## 13. Release Notes

### v2.0 — 28 เมษายน 2026
- Major: Dashboard / Material Management / NCR auto / Status separation
- Minor: History grouping, Approval split, Owner-check edit, Password lock

### v1.0 — 17 เมษายน 2026
- Initial release: Login, QC Entry (auto-fill, multi-defect, images), History, Edit approval, Admin panel, Bilingual UI, Guide

---

## 14. Contact & Support

- **Developer:** Datacomets (data.comets@gmail.com)
- **Source Code:** https://github.com/Datacomets/QC
- **Live URL:** https://web-mocha-three-44.vercel.app
- **Supabase Project:** https://supabase.com/dashboard/project/ruknpxlnvxgpraxkktfi
- **User Stories:** ดู [USER_STORIES.md](USER_STORIES.md)

---

*This PRD is a living document. Update version + date when features change.*
